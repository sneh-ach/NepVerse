{
  "buildCommand": "prisma generate && node scripts/ensure-prisma-engine.js && next build",
  "installCommand": "npm install --include=dev",
  "framework": "nextjs",
  "regions": ["iad1"],
  "functions": {
    "app/api/**/*.ts": {
      "includeFiles": "node_modules/.prisma/client/**"
    }
  }
}
